import { View, Text, SafeAreaView } from "react-native";
import React from "react";

type Props = {};

const Login = (props: Props) => {
  return (
    <View>
      <Text className="text-xl font-bold text-center">登录页</Text>
    </View>
  );
};

export default Login;
