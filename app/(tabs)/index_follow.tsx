import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const index_follow = (props: Props) => {
  return (
    <View>
      <Text style={styles.title}>index_follow</Text>
    </View>
  );
};

export default index_follow;

const styles = StyleSheet.create({
  title: {
    color: "red",
  },
});
